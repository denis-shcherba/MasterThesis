defaults:
  - _self_  
  - model: mlp
  - loss: default_3d
  - optimizer: adam
  - data: mlp
  - inference: mlp
train:
  batch_size: 32
  epochs: 100
  device: cuda

# Global settings
seed: 42
experiment_name: "SimpleSingleStepBC"
output_dir: ${hydra:runtime.output_dir}

wandb:
  enabled: false