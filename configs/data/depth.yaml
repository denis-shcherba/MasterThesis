# Path to the HDF5 file containing demonstrations
h5_file_path: demos/puck_real_augmented_1050.h5

# Data loading parameters
batch_size: ${train.batch_size}  
num_workers: ${train.num_workers}  

# Sequence settings
sequence_length: ${context_length} 
future_sequence_length: ${prediction_length}  
action_dim: 4       # Dimension of action space 

# Dataset split
train_split: 0.8    # Fraction of data to use for training

# Data processing options
is_regression: ${is_regression}  
is_waypointPlusTimings: ${is_waypointPlusTimings}  

# Optional parameters
random_seed: 42         # Random seed for reproducibility

#normalization options
normalize_obs: true  
normalize_actions: true
depth_normalization_method: minmax  
action_normalization_method: minmax
normalize_action_indices: [0, 1, 2, 3]
