defaults:
  - _self_  
  - model: mlp_head_simple
  - data: single_step
  - inference: default
  - env: default_delta3d
train:
  batch_size: 32
  epochs: 100
  device: cuda

experiment_name: "SimpleSingleStepBC Inference"

output_subdir: null
