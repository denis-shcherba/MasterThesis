defaults:
  - _self_  
  - model: regression
  - data: single_step
  - inference: default
  - env: default_regression
train:
  batch_size: 32
  epochs: 100
  device: cuda

experiment_name: "SimpleSingleStepBC Inference"

output_subdir: null
